using System.Collections;
using System.Collections.Generic;
using TMPro.EditorUtilities;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.UI;

public class Inventory
{
    //const int NUM_OF_KEYS = 3;
    //List<Item> _items = new List<Item>();
    //Key[] _keys = new Key[NUM_OF_KEYS];

    //public void AddItem(Item item)
    //{
    //    if(item != null)
    //        _items.Add(item);

    //    if (item.GetComponent<Key>() != null)
    //    {
    //        item.
    //        _keys.Add(item.GetComponent<Key>());
    //    }
    //}

    //public bool HasKey(string doorName)
    //{
    //    if (_keys.Length == 0)
    //        return false;

    //    for(int i = 0; i < _keys.Length; i++)
    //    {
    //        if (_keys[i].DoorName == doorName)
    //        {
    //            return true;
    //        }
    //    }

    //    return false;
    //}

    //public void UseKey(GameObject door)
    //{
    //    door.SetActive(false);

    //    for (int i = 0; i < _keys.Length; i++)
    //    {
    //        if (_keys[i].DoorName == door.name)
    //        {
    //            _keys.RemoveAt(i);
    //        }
    //    }
    //}

    //public void ShowKeySlot(GameObject keyInventory)
    //{
    //    음...키슬롯은 유아이로 가져올수있음. 글허면,,인벤토리를 받아와서 자식들에게 이미지가 잇으면 그 위에 이미지 올리면 더ㅣㅁ
    //    if(keyInventory != null &&  _keys.Count > 0)
    //    {
    //        int i = _keys.Count;
    //        while(i > 0)
    //        {
    //            keyInventory.GetComponent<Image>().sprite = 
    //        }
    //    }
    //}
}
